require 'spec_helper'

describe API do
  it 'initialize should raise an error with invalid endpoint' do
    #expect {
    #  API.new(nil, :endpoint => 'api.tester.com')
    #}.to raise_error(RuntimeError, /Invalid endpoint:/)
  end

  describe 'job' do

  end
end
